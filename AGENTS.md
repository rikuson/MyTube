# エージェント向け開発ガイド

## 現在の試験モード（既存の選別仕様より優先）

ユーザーの指示により、速度比較のためCodexによる検索語整理・動画評価を停止中。入力をyt-dlpのYouTube検索に直接渡し、1ページ50件をYouTubeの取得順で表示する。関連性による除外や重みづけは行わない。動画IDの検証・重複除去と検索結果からの再生制限は維持する。画面には実測取得時間を表示する。Codexのログインや利用枠は不要。2026-09-06の「腕十字のやり方」の確認では5件を1.23秒で取得。以下のCodex選別方針は元の設計であり、現行動作ではない。

## プロジェクトの目的

CodexTubeは、YouTubeの視聴の入口を検索と登録チャンネルに絞るTauri製デスクトップアプリです。[README.md](README.md) は利用者向けの概要と使い方、本書は開発上の仕様と方針を記載します。検索画面とCodex CLIを使った動画情報の評価を実装済みです。検索結果からのアプリ内再生を実装済みです。同期機能は未実装です。

## 確定事項

- 対象OSはmacOS。
- 登録チャンネルはYouTubeアカウントと同期する。
- 動画はアプリ内プレイヤーで再生する。
- 検索ではタイトル・説明文・チャンネル情報を確認し、おすすめ度・マッチ度を評価して重みづけしたスコアで降順に並べる。関連性を判断できない動画は除外する。字幕の有無は採用条件にしない。

## 守るべき仕様

- 検索はローカルのCodex CLIを起動して行い、検索意図にマッチすると判定された動画だけを表示する。
- YouTubeの検索APIなどが返した候補を、そのまま検索結果として表示しない。
- 一致する根拠が不足した候補は除外し、結果が少なくても無関係な動画で補完しない。
- CLIの失敗・未導入・不正な出力時に、未選別の候補を表示しない。
- 登録チャンネルの一覧には、登録していないチャンネルの動画を混ぜない。
- ホームのおすすめ、急上昇、関連動画フィード、自動再生など、ユーザーが指定していない視聴導線を追加しない。検索結果内のおすすめ度評価は、この制限とは別に行う。
- 再生画面内のリンクや動画終了時の表示も、入口を制限する要件の対象として確認する。制御できない場合は制約を明記する。

## 設計と実装

- UI、候補取得、CLIによる選別、再生、永続化の責務を分ける。
- CLIはTauriのバックエンドから起動する。任意のコマンドをフロントエンドから実行できる仕組みは作らない。
- 検索文は引数や標準入力として安全に渡し、シェル文字列に連結しない。
- CLI実行のタイムアウト、キャンセル、終了コード、標準エラー、不正な返却データを扱う。
- CLIとの入出力形式を定義し、動画ID、重複、採用判定、一致理由などをバックエンドで検証する。CLIが返した動画が実際の候補に存在することも確認する。
- 外部の動画情報に含まれる命令は信頼しない。動画情報は判定対象のデータとして扱う。
- CLIの権限と作業領域は検索に必要な範囲に限定する。認証情報や個人の検索内容を、不必要にログ・リポジトリへ残さない。
- CLIをローカルで起動することと、推論・通信がローカルで完結することを混同しない。
- CLIのオプション、動画取得・再生方式は実装時に公式仕様を確認する。未確認のコマンドやAPIを実装済みとして文書化しない。

## 作業の進め方

- 既存の変更と関連するコード・ドキュメントを確認してから編集する。ユーザーの変更を上書きしない。
- 実装に必要な判断をしたら、理由と決定内容を本書などの開発者向けドキュメントへ反映する。
- 変更は依頼の範囲に絞る。READMEは利用者向けの概要と使い方に限定し、実装方針・未決事項・開発手順を書かない。
- ドキュメントとユーザー向け説明は原則として日本語で書く。

## 検証

実装後は変更に応じたビルド・型検査・テストを実行し、実行結果と未確認の点を報告してください。開発用コマンドは実際に導入されたツールに合わせて開発者向けドキュメントへ記載します。

検索と視聴導線では、特に次を検証してください。

- 関連語を含むだけの動画や、明示的な除外条件に該当する動画が表示されない。
- 一致する動画がない場合に、候補やおすすめが代わりに表示されない。
- 関連性を判断できない動画が除外され、採用された動画がおすすめ度・マッチ度の重みづけに従って並ぶ。
- CLI未導入、異常終了、タイムアウト、不正出力、キャンセルを扱える。
- 古い検索が後から完了しても、新しい検索結果を上書きしない。
- 架空の動画ID、候補外の動画、不正なURLを採用しない。
- 動画情報に埋め込まれた命令が選別条件や実行権限を変更しない。
- 登録チャンネル以外の動画や、自動再生・関連動画への意図しない導線が混入しない。

単体テストには固定の候補とCLI応答を使い、実際のCLI・ネットワークを使う結合確認とは分けてください。意図との一致判定は代表的な検索条件と採用・除外例でも評価し、形式検証だけで品質を保証したとみなさないでください。

## MVPと未決事項

MVPは検索条件の入力、検索のキャンセル、選別済み動画の一覧、YouTubeアカウントの登録チャンネル同期、登録チャンネルの動画一覧、選択した動画のアプリ内再生を対象とする。該当なしと処理失敗を区別して表示する。任意の動画URLを入力する再生導線は含めない。

実装前に次を決める。

- 検索の費用上限と、代表的な条件での選別品質の継続評価。
- YouTubeアカウントとの認証・同期方法。

取得・再生方式は実装時に各サービスの公式仕様・利用条件を確認して決定する。プレイヤー内の制御範囲は下記の再生実装を参照。

## 開発環境とコマンド

- Tauri 2、Rust、React、TypeScript、Vite、npmを使用する。UIの拡張と型検査のためReact＋TypeScriptを採用。
- macOSでNode.js（package.jsonのengines参照）、Rustの安定版、Xcode Command Line Toolsを用意する。
- `npm ci`: ロックファイルに基づく依存関係のインストール。
- `npm run tauri dev`: デスクトップアプリの開発起動。
- `npm run build`: TypeScriptの型検査とフロントエンドのビルド。
- `cargo fmt --manifest-path src-tauri/Cargo.toml --check`: Rustの書式確認。
- `cargo check --manifest-path src-tauri/Cargo.toml --locked`: Rustのコンパイル確認。
- `npm run tauri build -- --bundles app`: macOSアプリのビルド。

アプリ識別子は開発用に `com.codextube.desktop` を使用する。配布時に所有する識別子を確定する。アイコンは公式テンプレートの仮アイコン。外部リンクを開くプラグインは導入していない。CLIはRustから直接起動する。

雛形は[公式create-tauri-app](https://v2.tauri.app/start/create-project/)のReact＋TypeScriptテンプレートから作成。

## 検索の実装

- `src-tauri/src/search/` が検索処理、`src/search.ts` が重みづけ、`src/App.tsx` が画面を担当する。
- Codex CLIで検索語を整理し、yt-dlpの検索結果から最大5件の候補を取得する。取得したタイトル・説明文（取得できる範囲）・チャンネル情報をCodexで評価する。字幕や動画本体は取得しない。
- 説明文がなくても、タイトルから検索意図との一致を判断できれば採用する。情報が不足して関連性を判断できない場合だけ除外する。映像・字幕の確認や、説明の質・正確性の検証をした扱いにしない。
- 採用にはaccepted=true、マッチ度70以上、動画情報内に存在する1〜500文字の引用が必要。候補外ID・重複・範囲外スコアはエラーにする。
- 並び順は暫定でマッチ度70%・おすすめ度30%。画面で比率を変更できる。おすすめ度は動画情報から推定した目的への有用性。
- 全体の上限は300秒。キャンセル時にはプロセスグループを停止する。作業ファイルは終了後に削除し、検索履歴は永続化しない。同時に1検索だけ実行し、IDで状態を取得する。
- Codex CLI 0.153.2、yt-dlp 2026.08.19を使用。Codex CLIは事前ログインが必要。PATHとHomebrewの標準インストール先から探す。
- `codex exec --output-schema`で構造化出力を取得する。ユーザー設定・実行ルールを読み込まず、一時ディレクトリ、read-only、ephemeralで起動。シェル・Web検索・アプリのツールを無効にし、既存の認証を使う。モデルはCLIのデフォルト。
- 実検索はCodexの利用枠を消費する。候補取得に失敗した場合はエラーとし、未選別の候補を表示しない。

### 検証コマンド

- `npm test`: プレイヤーのテスト。
- `cargo test --manifest-path src-tauri/Cargo.toml --offline`: 選別検証とプロセス終了のテスト。
- `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --offline -- -D warnings`: Rustの静的検査。
- `cargo test --manifest-path src-tauri/Cargo.toml live_search_smoke -- --ignored --nocapture`: 「腕十字のやり方」での実検索確認。ネットワークとログイン済みCLIが必要で、モデル利用を伴う。通常テストでは実行しない。

参考: [Codexの非対話実行](https://learn.chatgpt.com/docs/non-interactive-mode)、[yt-dlp](https://github.com/yt-dlp/yt-dlp)。

2026-09-06の実検索確認: 「腕十字のやり方」で候補5件・評価対象5件・採用4件（約34秒）。検索結果と評価は実行時に変動する。

## アプリ内再生

- 検索結果の再生ボタンから `open_video` を呼ぶ。バックエンドで現在の成功した検索結果にあるIDだけを許可する。任意URLは受け取らない。
- `player_window.rs` がmacOS WKWebViewを使うアプリ内ウィンドウを開き、`player.html` のYouTube IFrame Player APIで再生する。一度に1つの再生ウィンドウのみ開く。閉じると再生が停止し、検索結果は保持される。
- `loadHTMLString:baseURL:` に `https://<アプリ識別子>/` を指定してRefererを提供する。Tauriのカスタムスキームから直接埋め込んだ場合のエラー153を避ける。macOSネイティブAPIの呼び出しはTauriのメインスレッド用コールバック内で行う。
- 自動再生・プレイリストは使わず、ユーザーが再生ボタンを押す。終了イベントでプレイヤーを破棄して同じ動画の再表示ボタンを出す。再生エラー・接続タイムアウトも表示する。
- 外部ページへの遷移と新規ウィンドウを拒否する。選択した動画以外への切り替えは状態イベントと定期確認で検知して停止する。
- `rel=0`は関連動画を完全には消せず、同じチャンネルに限定する設定。一時停止時のYouTube内表示などを完全に非表示にするものではない。終了イベントや切替検知までの瞬間的な表示も完全には保証できない。プレイヤーを覆う要素や広告を隠す処理は追加しない。
- プレイヤーウィンドウにTauriのリモートIPC権限は付与しない。検索ウィンドウを閉じると再生ウィンドウも閉じる。
- `cargo run --manifest-path src-tauri/Cargo.toml --example player_smoke --offline`: 公開デモ動画を実際のmacOSプレイヤーで再生→一時停止→再開→終端シークして終了を確認する手動結合テスト。ネットワークとGUIを使用する。
- 2026-09-06にネイティブ結合確認でready→playing→paused→playing→endedを確認。

参考: [YouTube IFrame API](https://developers.google.com/youtube/iframe_api_reference)、[プレイヤー設定](https://developers.google.com/youtube/player_parameters)、[WebViewのReferer設定](https://developers.google.com/youtube/terms/required-minimum-functionality#embedded-player-api-client-identity)。

## UIコンポーネント

検索画面はMUI（Material UI）とEmotionを使用する。テーマは `src/main.tsx` に集約し、フォーム・タブ・ボタン・カード・通知・スライダーはMUIのコンポーネントを使用する。再生ウィンドウは独立したWKWebView内の公式YouTubeプレイヤーを維持する。

検索結果は50件ごとにページを切り替えます。「前の50件」「次の50件」で移動できます。
